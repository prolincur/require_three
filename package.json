{
  "name": "require_three",
  "version": "0.129.0",
  "main": "src/cdn.js",
  "scripts": {
    "build": "yarn build:bundle && yarn build:cdn && yarn build:local",
    "build:cdn": "node src/cdn.js",
    "build:local": "node src/local.js",
    "build:bundlables": "node src/bundle.js",
    "build:bundle": "gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sourabh Soni",
  "license": "Private",
  "devDependencies": {
    "copy-dir": "^1.3.0",
    "gulp": "^4.0.2",
    "gulp-debug": "^4.0.0",
    "gulp-livereload": "^4.0.2",
    "js-beautify": "^1.6.8",
    "sync-request": "^4.0.1",
    "three": "0.129.0",
    "uglify-js": "^2.7.5",
    "webpack-stream": "^6.1.2"
  },
  "dependencies": {}
}
